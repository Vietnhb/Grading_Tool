name: grading_rubric_parser
description: A small Dart library to parse DOCX grading guides into JSON rubrics and generate LLM prompts.
version: 0.1.0
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  xml: ^6.1.0
  archive: ^3.3.2
  path: ^1.8.2
